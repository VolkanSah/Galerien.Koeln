{
  "name": "dein-projektname",
  "version": "1.0.0",
  "description": "Ein statisches Website-Projekt mit Tailwind CSS",
  "devDependencies": {
    "tailwindcss": "^3.4.1"
  },
  "scripts": {
  "build": "npx tailwindcss -i ./input.css -o ./dist/tailwind.css --minify"
  }
}
